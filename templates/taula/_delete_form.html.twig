<form method="post" action="{{ path('taula_delete', {'id': taula.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taula.id) }}">
    <button class="btn">Delete</button>
</form>
