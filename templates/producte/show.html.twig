{% extends 'base.html.twig' %}

{% block title %}Producte{% endblock %}
{% block nomBar %} {{ barLogged.nom }} {% endblock %}
{% block body %}
    <h1>Producte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producte.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ producte.nom }}</td>
            </tr>
            <tr>
                <th>Preu</th>
                <td>{{ producte.preu }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ producte.categoria }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ producte.disponible ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producte_index') }}">back to list</a>

    <a href="{{ path('producte_edit', {'id': producte.id}) }}">edit</a>

    {{ include('producte/_delete_form.html.twig') }}
{% endblock %}
